import React from 'react';

// Este é um componente placeholder para corrigir o erro de build.
// O conteúdo original do DashboardPrincipal precisa ser restaurado.
const DashboardPrincipal = (props: any) => {
  return (
    <div className="p-4 bg-white rounded-lg shadow">
      <h2 className="text-xl font-bold">Dashboard Principal (Placeholder)</h2>
      <p>O conteúdo original deste componente foi perdido e precisa ser restaurado.</p>
    </div>
  );
};

export default DashboardPrincipal;
